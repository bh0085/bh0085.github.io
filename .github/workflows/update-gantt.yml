name: Update Gantt Data

# Automatically fetches latest task data from Notion database and updates gantt_data.json
# The gantt.html and index.html pages read from this JSON file to display the chart
# Database ID: 291cdd12fc5e81ceafeadc78e6c03258

on:
  schedule:
    - cron: '0 0 * * *'  # Run daily at midnight UTC
  workflow_dispatch:  # Allow manual trigger from Actions tab

jobs:
  update-gantt:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Fetch Gantt data from Notion
        env:
          NOTION_API_KEY: ${{ secrets.NOTION_API_KEY }}
        run: |
          python fetch_gantt.py

      - name: Commit updated data
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git add gantt_data.json
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update Gantt chart data from Notion [automated]"
            git push
          fi

